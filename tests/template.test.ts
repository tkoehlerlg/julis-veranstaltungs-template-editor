import { test, expect } from '@playwright/test'

// test('click on header', async ({ page }) => {
//     await page.goto('/')
//
//     // Expect a title "to contain" a substring.
//     const header = await page.getByText('Test')
//     await expect(header).toHaveText('Test', { timeout: 5000 })
// })
